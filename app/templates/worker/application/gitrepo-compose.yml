services:
  ${APPLICATION_NAME}:
    build:
      context: ${REPO}
      dockerfile: ${DOCKERFILE}
      args: [ ${BUILD_ARGS} ]
    container_name: ${CONTAINER_NAME}
    restart: unless-stopped
    env_file: .env
    networks:
      - sage_default

networks:
  sage_default:
    external: true